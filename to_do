broadcast message to all channels if nick name changed

two clients connected and the first disconnectedcompile in codespaces with all flags + fsanitize

const for the getters

9ad folder dial commands folder dial auth...


when the operator of the channel exits we set a random member as operator

broadcast nick change to all users

last user f channel kanm7i channel


finma kayn map khasni nchecki wach lkey valid



ctrl c w n3awd ndkhl w njoini chi channel kant kadir segv


lockiw webserv w diro give up

 Channels names are strings (beginning with a '&' or '#' character) of
   length up to 200 characters.  Apart from the the requirement that the
   first character being either '&' or '#'; the only restriction on a
   channel name is that it may not contain any spaces (' '), a control G
   (^G or ASCII 7), or a comma (',' which is used as a list item
   separator by the protocol).


   but a limit of ten (10)
   channels is recommended as being ample for both experienced and